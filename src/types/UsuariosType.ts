export type UsuariosType = {
  uscodigo?: string;
  usnome: string;
  usemail: string;
  ussenha: string;
  usnascimento: string;
  usfoto: string;
  createdAt?: string;
  updatedAt?: string;
};

export type EsqueciSenhaType = {
  email: string;
};
